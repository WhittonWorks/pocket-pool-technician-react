import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.whittonworks.ppt',
  appName: 'Pocket Pool Technician',
  webDir: 'build'
};

export default config;
